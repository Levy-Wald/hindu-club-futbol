export function footerConfidencial(usuario: string, fecha: string): string {
  return `CONFIDENCIAL — Datos de salud protegidos. Exportado por ${usuario} el ${fecha}. Distribución no autorizada prohibida.`
}
